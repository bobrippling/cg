# test=harness 3

$glob = [i4 x 2]

$test = i4()
{
	$local = load $glob

	$glob_off = elem $glob, 4

	store $glob_off, i4 3

	$glob_off_read = load $glob_off

	$added = add $local, $glob_off_read

	ret $added
}
