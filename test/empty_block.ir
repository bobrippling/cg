# test=sh $ir --emit=ir $f | awk 'BEGIN{x=0} {if(x){print}} /^[^B]/{x=0} /^B:/{x=1}' | grep 'jmp fin' >/dev/null

4 main()
{
	x = load.4 0
	y = load.4 0

	cond = eq.4 x, y
	br cond, A, B

A:
	jmp fin

B:

fin:
	ret.4 7
}
