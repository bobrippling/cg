# test=sh test $($ir --emit=ir -O $f | grep -c global_var) -eq 2

$global_var = i4

$loadmerge = i4()
{
	$a = load $global_var
	$b = load $global_var
	$c = load $global_var

	$x = add $a, $b
	$y = add $x, $c

	ret $y
}
